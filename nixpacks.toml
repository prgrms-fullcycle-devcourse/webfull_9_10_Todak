# Railway(Nixpacks) 배포용 설정.
# 기본 Nixpacks 는 install(=devDeps 포함) 한 node_modules 를 그대로 런타임 이미지에 담는다.
# 통합테스트 전용 devDependency 인 embedded-postgres(~140MB Postgres 바이너리) 등이
# 이미지에 끼어 export 가 매우 느려지므로, build 후 devDeps 를 정리한다.
[phases.prune]
dependsOn = ["build"]
cmds = ["pnpm prune --prod"]
